jwt:
  secret: nutee
spring:
  config:
    use-legacy-processing: true
  application:
    name: auth
  profiles:
    active: local
  cloud:
    config:
      uri: http://3.34.61.71:8888
eureka:
  instance:
    instance-id: ${spring.cloud.client.hostname}:${spring.application.instance_id:${random.value}}
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone : http://127.0.0.1:8761/eureka
